Stage 1 (Detection): Use your professor's existing trained YOLO model to detect text regions in vital records
Stage 2 (OCR): Add an off-the-shelf OCR model (no training required) to transcribe the text within detected bounding boxes
Integration: Create a complete end-to-end system that processes full documents and outputs both locations and transcribed text
Evaluation: Focus on overall system performance (detection + recognition combined)

Your Novel Contribution: Completing the OCR component that your professor's research was missing, creating a full working system for multilingual handwriting recognition in vital records.
Immediate Next Steps:

Research and select the best off-the-shelf OCR library for handwritten text
Create a small annotated dataset for testing (bounding boxes + transcriptions)
Implement the pipeline integration
Evaluate end-to-end performance